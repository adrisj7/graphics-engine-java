# Command Rules.
# Usage:
# Each line is one command.
# [COMMAND HERE] ARG0 ARG1 ...

# Commands wrapped in <> are optional.
# For example:
# COMMAND <NUMBER> STRING STRING can be called as either "COMMAND hello world" or "COMMAND 245 hey universe"

# Make sure to update Parser.java's COMMAND array when you make changes here!

NULL
FRAMES NUMBER
BASENAME STRING
BASESIZE NUMBER NUMBER
BACKGROUND NUMBER NUMBER NUMBER
VARY STRING NUMBER NUMBER NUMBER NUMBER
LIGHT <STRING> NUMBER NUMBER NUMBER NUMBER NUMBER NUMBER
AMBIENT NUMBER NUMBER NUMBER
PUSH
POP
MOVE NUMBER NUMBER NUMBER <KNOB>
SCALE NUMBER NUMBER NUMBER <KNOB>
ROTATE STRING NUMBER <KNOB>
SPHERE NUMBER NUMBER NUMBER NUMBER
BOX NUMBER NUMBER NUMBER NUMBER NUMBER NUMBER <STRING>
TORUS NUMBER NUMBER NUMBER NUMBER NUMBER
SAVE